language: node_js
node_js:
- node
cache: npm
script:
  - cd frontend
  - npm install
  - npm run build
  - cd dist
  - cd ../../backend/
  - mv docker-compose.yml dockerfile requirements.txt Server.py ../frontend/dist
  - cd ../frontend
  - npm run deploy
